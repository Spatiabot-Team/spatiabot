import {SocialLocalInterface} from "../interfaces/social-local.interface";

export class SocialLocal implements SocialLocalInterface {
    id?: string;
    username?: string;
    password?: string;
}
