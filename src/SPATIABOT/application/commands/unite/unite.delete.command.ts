export class UniteDeleteCommand {

    uniteId: string;
    auteurId: string;

    constructor(uniteId: string, auteurId: string) {
        this.uniteId = uniteId;
        this.auteurId = auteurId;
    }
}
